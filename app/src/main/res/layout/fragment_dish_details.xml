<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.fragments.dishdetails.DishDetailsFragment"
    tools:ignore="ContentDescription">

    <include
        android:id="@+id/i_dish_details"
        layout="@layout/dish_details_layout" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end">

        <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
            android:id="@+id/fab_edit_dish"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/dim_fab_margin_end"
            android:layout_marginBottom="@dimen/dim_fab_margin_vertical"
            android:text="@string/st_edit"
            app:icon="@drawable/ic_edit_24"
            app:tint="?attr/colorPrimary" />

        <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
            android:id="@+id/fab_delete_dish"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="@dimen/dim_fab_margin_end"
            android:layout_marginBottom="@dimen/dim_fab_margin_vertical"
            app:icon="@drawable/ic_delete_24"
            app:tint="?attr/colorPrimary" />
    </LinearLayout>
</FrameLayout>